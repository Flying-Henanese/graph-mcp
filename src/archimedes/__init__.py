"""
Archimedes MCP Server Package.

A minimalist, local Model Context Protocol (MCP) server designed to act as
"X-ray glasses" for Large Language Models. It dynamically strips Python code
implementation details, returning only the "skeleton" (interfaces, signatures,
dependencies) to provide high-level context with minimal token usage.
"""
